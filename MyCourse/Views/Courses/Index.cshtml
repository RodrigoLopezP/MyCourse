@model CourseListViewModel
<div class="row">
    <div class="col-md-8">
        <h1>@ViewBag.Title</h1>
    </div>
    <div class="col-md-4 d-flex align-items-center">
        <form asp-controller="Courses" asp-action="Index" method="get">
            <div class="input-group">
                <input class="form-control" type="text" name="search" placeholder="Cerca..."
                    value="@Model.Input.Search">
                <div class="input-group-append">
                    <button class="input-group-text"><i class="fas fa-dog"></i></button>
                </div>
            </div>
        </form>
    </div>
</div>
<div class="row">
    <div class="col-md-1"></div>
    <div class="col-md-5">
        <order-link order-by="Title" input="@Model.Input">Title</order-link>
    </div>
    <div class=" col-md-2">
          <order-link order-by="Rating" input="@Model.Input">Valutazione</order-link>
    </div>
    <div class="col-md-2">
          <order-link order-by="CurrentPrice" input="@Model.Input">Prezzo</order-link>
    </div>
    <div class="col-md-2"></div>
</div>
@foreach (CourseViewModel course in Model.Courses.Results)
{
    <hr>
    <div class="row course">
        <div class="col-md-1">
            <img class="img-fluid" src="@course.ImagePath" height="60" alt="Img corso 1">
        </div>
        <div class="col-md-5">
            <h2>@course.Title</h2>
            <author>di @course.Author</author>
        </div>
        <div class="col-md-2 rating" title="@course.Rating">
           <rating value="@course.Rating"> </rating>
        </div>
        <div class="col-md-2 price">
            <price full-price="@course.FullPrice" current-price="@course.CurrentPrice"></price>
        </div>
        <div class="col-md-2">
            <a class="btn btn-outline-primary" asp-action="Detail" asp-route-id="@course.Id"> Dettaglio</a>
        </div>
    </div>
    <hr>
}
<vc:pagination-bar model="@Model"></vc:pagination-bar>